# 香港建筑资产年龄数据分析与 AI 风险摘要生成 - 深化需求文档

## 1. 项目目标

本项目目标是做出一个可真实运行、可复现、可交付的 v1.0 dashboard 产品，用于分析香港建筑资产的年龄结构，并结合区域、建筑用途、监管通知等公开数据生成建筑老化风险摘要和维护优先级报告。

系统不以临时演示 MVP 为最终目标，而是直接以 v1.0 成品交付为目标。工程上可以分阶段推进，但最终需要形成完整产品闭环：

- 接入和缓存香港官方建筑年龄、18 区边界、MBIS 监管通知等公开数据。
- 按区域、建筑用途、建筑类型、楼龄区间和风险等级进行清洗、筛选和统计。
- 用多页面 dashboard 展示建筑老化、区域优先级、监管信号和数据质量。
- 根据可解释规则进行风险分层，并保留风险解释和模型版本。
- 用模板摘要和 LLM 摘要生成中文或英文风险报告、维护建议和数据局限说明。
- 支持 Markdown / HTML 报告导出，并保留数据源版本、模型版本和 prompt 版本。

## 2. 使用对象

主要使用对象包括：

- 建筑、城市规划、房地产和基础设施方向的研究者。
- 需要快速理解建筑资产老化风险的学生或分析人员。
- 做香港建筑资产、城市更新、维护优先级研究的项目团队。
- 需要将数据分析结果转化为报告文本的非技术用户。

## 3. 输入数据

### 3.1 必需输入

系统至少需要支持一个 CSV 文件，建议字段如下：

| 字段名 | 含义 | 示例 |
| --- | --- | --- |
| building_id | 建筑唯一编号 | BLDG_0001 |
| building_name | 建筑名称，可为空 | Mei Foo Sun Chuen |
| district | 区域或行政区 | Yau Tsim Mong |
| address | 地址，可为空 | Nathan Road |
| building_use | 建筑用途 | Residential / Commercial / Industrial / Mixed |
| completion_year | 落成年份 | 1978 |
| latitude | 纬度 | 22.3193 |
| longitude | 经度 | 114.1694 |

### 3.2 可选输入

后续可扩展字段：

- floor_count：楼层数。
- gross_floor_area：建筑面积。
- ownership_type：公营、私营、混合。
- last_major_repair_year：最近大修年份。
- inspection_status：是否已完成近期检测。
- heritage_status：是否有历史建筑或保护状态。
- occupancy_level：使用强度或入住率。

### 3.3 数据质量要求

导入后需要进行基础校验：

- completion_year 必须是合理年份，例如 1800 至当前年份。
- latitude 和 longitude 需要落在香港大致范围内。
- district 和 building_use 不能为空；为空时标记为 Unknown。
- 重复 building_id 需要提示，并保留第一条或让用户下载异常记录。
- 无法计算楼龄的数据不参与风险评分，但需要在数据质量摘要中列出。

## 4. 核心功能

### 4.1 数据导入与字段识别

用户进入系统后，可以上传 CSV 文件。系统读取后展示：

- 数据总行数。
- 可识别字段。
- 缺失值统计。
- 异常 completion_year 数量。
- 缺失经纬度数量。

如果字段名不完全一致，系统应提供简易字段映射逻辑，例如把 `year_built`、`built_year`、`completion` 识别为 completion_year。

### 4.2 数据清洗

系统需要自动完成以下清洗：

- 标准化字段名。
- 统一区域名称大小写和空格。
- 统一建筑用途分类。
- 根据 completion_year 计算 building_age。
- 按楼龄划分 age_band：
  - 0-20 年：较新建筑。
  - 21-40 年：中龄建筑。
  - 41-60 年：老化关注建筑。
  - 60 年以上：高龄建筑。
- 过滤明显错误值，并生成数据质量提示。

### 4.3 筛选功能

页面需要提供以下筛选条件：

- 区域 district。
- 建筑用途 building_use。
- 楼龄区间 age_band。
- 风险等级 risk_level。

筛选后，图表、地图和 AI 摘要都应基于当前筛选结果更新。

### 4.4 统计分析

系统需要输出以下统计指标：

- 建筑总数。
- 平均楼龄。
- 中位楼龄。
- 40 年以上建筑占比。
- 60 年以上建筑占比。
- 各区域建筑数量排行。
- 各用途建筑数量排行。
- 各楼龄区间占比。
- 高风险建筑数量及占比。

### 4.5 风险分层

v1.0 采用可解释的规则评分，不使用黑箱模型。

建议评分逻辑如下：

| 因素 | 规则 | 分值 |
| --- | --- | --- |
| 楼龄 | 0-20 年 | 10 |
| 楼龄 | 21-40 年 | 30 |
| 楼龄 | 41-60 年 | 60 |
| 楼龄 | 60 年以上 | 85 |
| 用途 | Residential | +5 |
| 用途 | Commercial | +10 |
| 用途 | Industrial | +15 |
| 用途 | Mixed | +12 |
| 缺少最近维修年份 | last_major_repair_year 为空 | +10 |
| 最近维修较久 | 距今超过 20 年 | +10 |

风险等级：

- 0-30：Low，低风险。
- 31-60：Medium，中风险。
- 61-80：High，高风险。
- 81-100：Critical，重点关注。

所有分数需要限制在 0 到 100 之间。系统界面必须说明风险评分是初步筛查结果，不等同于工程检测结论。

### 4.6 地图可视化

地图至少支持一种方式：

- 点位地图：每栋建筑作为一个点，颜色表示风险等级。
- 区域聚合地图：按 district 聚合，颜色表示平均风险或高龄建筑占比。

v1.0 默认使用 folium，因为它和 Streamlit 集成简单，适合快速交付。后续如果需要更强交互，可以改用 plotly mapbox 或 pydeck。

地图交互要求：

- 点击建筑点显示建筑名称、区域、用途、楼龄、风险等级。
- 图例清晰区分 Low、Medium、High、Critical。
- 筛选条件变化后地图同步更新。

### 4.7 图表可视化

建议提供以下图表：

- 楼龄区间柱状图。
- 风险等级柱状图。
- 区域维度的高风险建筑数量排行。
- 建筑用途与风险等级交叉统计图。
- 楼龄分布直方图。

v1.0 可使用 plotly express，便于 Streamlit 中交互展示。

### 4.8 AI 风险摘要生成

AI 摘要不直接读取原始全部数据，而是读取统计后的结构化摘要，降低 token 成本并减少隐私风险。

输入给 LLM 的内容包括：

- 当前筛选条件。
- 建筑总数、平均楼龄、中位楼龄。
- 40 年以上和 60 年以上建筑占比。
- 风险等级分布。
- 高风险区域排行。
- 高风险用途排行。
- 数据质量提示。

AI 输出建议分为四段：

1. Overall Risk Summary：总体风险摘要。
2. Key Observations：关键发现。
3. Maintenance Priorities：维护优先级建议。
4. Data Limitations：数据局限和后续需要补充的数据。

AI 输出需要强调：

- 结果是数据分析和规则评分，不是正式结构安全鉴定。
- 如用于真实决策，应结合现场检测、结构资料和维护历史。

### 4.9 报告导出

v1.0 需要支持导出 Markdown 和 HTML 报告。

报告内容包括：

- 项目标题。
- 数据范围与筛选条件。
- 核心统计指标。
- 图表截图或图表说明。
- 风险等级解释。
- AI 生成摘要。
- 数据质量说明。

后续可扩展为 PDF 导出。

## 5. 页面流程

建议使用 Streamlit 做单页应用，页面结构如下：

1. 页面标题和简短说明。
2. 数据上传区。
3. 数据质量概览。
4. 左侧筛选栏：
   - district。
   - building_use。
   - age_band。
   - risk_level。
5. 核心指标卡片：
   - 总建筑数。
   - 平均楼龄。
   - 40 年以上占比。
   - 高风险占比。
6. 地图区域。
7. 图表分析区域。
8. AI 摘要生成按钮。
9. 报告预览和导出按钮。

## 6. 推荐技术方案

### 6.1 技术栈

- Python：主语言。
- pandas：CSV 读取、清洗、统计。
- geopandas：如后续需要处理区界 GeoJSON，可加入。
- folium：地图展示。
- streamlit-folium：把 folium 地图嵌入 Streamlit。
- plotly：交互式图表。
- Streamlit：快速构建本地分析 dashboard 产品。
- OpenAI API 或本地 LLM 接口：生成风险摘要。
- python-dotenv：管理 API Key。

### 6.2 建议项目结构

```text
project/
  app.py
  requirements.txt
  .env.example
  data/
    sample_buildings.csv
  src/
    data_loader.py
    cleaning.py
    risk_model.py
    analytics.py
    visualization.py
    report_generator.py
    llm_summary.py
  outputs/
    reports/
  README.md
  需求文档.txt
```

### 6.3 模块职责

| 模块 | 职责 |
| --- | --- |
| app.py | Streamlit 页面入口，组织上传、筛选、展示和导出 |
| data_loader.py | 读取 CSV、字段映射、基础校验 |
| cleaning.py | 数据清洗、楼龄计算、分类标准化 |
| risk_model.py | 风险评分和风险等级划分 |
| analytics.py | 指标统计和聚合分析 |
| visualization.py | 地图和图表生成 |
| llm_summary.py | 组织 prompt，调用 LLM，返回摘要 |
| report_generator.py | 生成 Markdown / HTML 报告 |

## 7. LLM Prompt 设计

建议使用结构化 prompt，避免让模型自由发挥。

```text
You are an urban building asset risk analyst.

Based on the following structured statistics, write a concise risk summary for Hong Kong building assets.

Requirements:
- Do not claim this is a formal structural inspection.
- Focus on aging, district-level concentration, building use, and maintenance priority.
- Include data limitations.
- Output in Chinese unless the user selects English.

Input statistics:
{statistics_json}

Output structure:
1. Overall Risk Summary
2. Key Observations
3. Maintenance Priorities
4. Data Limitations
```

为了方便测试，也需要准备一个 fallback 模式：如果没有配置 API Key，则根据模板自动生成一版规则化摘要，保证系统演示不中断。

## 8. v1.0 开发步骤

### 第一步：准备样例数据

先准备 `data/sample_buildings.csv`，包含 100 至 500 条模拟或公开整理数据，字段符合第 3 节要求。

验收标准：

- CSV 可以被系统成功读取。
- 字段缺失和异常值可以被识别。
- 可以计算 building_age 和 age_band。

### 第二步：完成数据清洗与风险评分

实现清洗逻辑和 risk_score / risk_level 计算。

验收标准：

- 每条有效建筑都有 building_age、age_band、risk_score、risk_level。
- 风险评分可解释。
- 异常数据不会导致系统崩溃。

### 第三步：完成 Streamlit 页面

实现上传、筛选、指标卡片、地图和图表展示。

验收标准：

- 用户可上传 CSV。
- 用户可按区域、用途、楼龄、风险等级筛选。
- 所有图表会随筛选结果刷新。

### 第四步：完成 AI 摘要

将筛选后的统计结果转成 JSON，输入 LLM，生成风险摘要。

验收标准：

- 点击按钮后生成结构化摘要。
- 没有 API Key 时也有模板摘要。
- 摘要中包含风险说明和数据局限。

### 第五步：完成报告导出

将当前分析结果和 AI 摘要导出为 Markdown 或 HTML。

验收标准：

- 可以生成报告文件。
- 报告包含筛选条件、指标、风险说明、AI 摘要。

## 9. 验收标准

v1.0 产品基线完成后，应满足：

- 可以在本地通过 `streamlit run app.py` 启动。
- 可以上传香港建筑年龄 CSV。
- 可以完成数据清洗、楼龄计算和风险分层。
- 可以通过地图查看建筑风险分布。
- 可以通过图表理解区域、用途和楼龄结构。
- 可以生成一段有结构的 AI 风险摘要。
- 可以导出基础分析报告。
- 系统遇到缺失字段、空数据、异常年份时不会直接崩溃，而是给出提示。

## 10. 非目标范围

v1.0 暂不做：

- 用户登录和权限管理。
- 数据库后台。
- 多人协作。
- 自动从政府网站抓取数据。
- 正式工程结构安全评估。
- 复杂机器学习预测模型。
- PDF 高级排版。

这些功能可以在 v2.0 或平台化阶段扩展。

## 11. 后续扩展方向

第二阶段可以考虑：

- 引入香港区界 GeoJSON，做 district 级 choropleth 地图。
- 增加维修历史和检查记录，提高风险评分可信度。
- 增加建筑密度、人口密度、土地用途等外部变量。
- 接入公开数据源，减少手工上传。
- 支持 PDF 报告导出。
- 支持中英文切换。
- 支持多版本风险规则配置。
- 加入真实案例库，让 AI 摘要更贴近建筑维护语境。

## 12. 初步落地结论

本项目最适合先做成本地 Streamlit dashboard 产品，而不是一开始做完整 Web 平台。原因是当前目标以数据分析、可视化和报告生成为主，Streamlit 能最快把官方数据、pandas、地图、图表和 LLM 摘要串起来。

推荐落地路径：

1. 先用样例 CSV 跑通端到端流程。
2. 再替换为真实香港建筑年龄数据。
3. 优先完善风险规则和报告质量。
4. 等 v1.0 成品稳定后，再考虑数据库、用户系统和公网部署。

这样可以在较短时间内得到一个可运行、可交付、可用于研究汇报的 dashboard 产品。

## 13. 需求优先级

为了保证项目能快速落地，需求分为 P0、P1、P2 三个层级。

| 优先级 | 含义 | 进入 v1.0 |
| --- | --- | --- |
| P0 | 没有该功能，系统无法完成核心演示 | 是 |
| P1 | 对系统完整性和展示效果很重要，但可在 P0 后补充 | 尽量做 |
| P2 | 增强体验、扩展能力或长期平台化能力 | 后续做 |

### 13.1 P0 功能

- CSV 上传和读取。
- 字段标准化和基础数据校验。
- completion_year 转 building_age。
- age_band 分类。
- risk_score 和 risk_level 计算。
- 区域、用途、楼龄、风险等级筛选。
- 核心指标展示。
- 点位地图展示。
- 风险等级和楼龄分布图表。
- 基于统计结果生成 AI 风险摘要。
- Markdown 报告导出。

### 13.2 P1 功能

- 自动字段映射确认界面。
- 异常数据下载。
- 区域级聚合地图。
- 多图表联动分析。
- 中英文报告切换。
- 自定义风险权重。
- HTML 报告导出。
- 使用本地模板在无 API Key 时生成摘要。

### 13.3 P2 功能

- 数据库持久化。
- 用户登录。
- 历史项目管理。
- PDF 报告导出。
- 自动接入公开数据源。
- 接入维修记录、检测记录和资产估值数据。
- 多版本模型比较。
- 云端部署和多人协作。

## 14. 用户故事与使用场景

### 14.1 上传数据并快速了解整体老化情况

作为研究者，我希望上传一份香港建筑年龄 CSV 后，系统自动告诉我数据里建筑的平均楼龄、老龄建筑占比和高风险建筑占比，这样我可以快速判断数据集是否有明显老化问题。

验收条件：

- 上传 CSV 后 5 秒内显示基础统计。
- 如果字段缺失，页面明确提示缺少哪些字段。
- 如果数据为空或全是异常年份，页面给出可理解错误提示。

### 14.2 筛选某个区域的风险

作为城市更新分析人员，我希望选择某个 district 后，只看该区域的建筑楼龄分布、风险等级分布和 AI 摘要，这样可以为区域级维护策略提供依据。

验收条件：

- district 筛选后，指标、地图、图表、AI 摘要输入数据同步更新。
- 页面显示当前筛选条件。
- 筛选结果为空时不报错，并提示用户调整筛选条件。

### 14.3 比较不同建筑用途的老化风险

作为资产管理人员，我希望按 Residential、Commercial、Industrial、Mixed 等用途比较风险等级，从而判断哪类建筑更需要优先关注。

验收条件：

- 系统提供用途维度的风险交叉统计图。
- AI 摘要能指出高风险用途是否集中。
- 报告中保留用途筛选条件和用途风险发现。

### 14.4 生成可放入研究汇报的文字摘要

作为学生或研究助理，我希望系统把数据分析结果转成一段结构清晰的报告文字，而不是只给我图表。

验收条件：

- AI 摘要包含总体判断、关键发现、维护优先级、数据局限。
- 摘要不夸大为正式结构安全结论。
- 摘要可以复制，也可以进入导出报告。

## 15. 数据来源与落地策略

### 15.1 第一阶段数据策略

第一阶段优先使用样例数据或用户手动整理的 CSV，确保系统流程先跑通。样例数据可以来自三类方式：

- 手工构造模拟数据，用于开发和演示。
- 用户已有研究数据，整理成系统要求字段。
- 公开资料整理后的建筑清单，但 v1.0 不要求系统自动抓取网页，只接入明确的官方数据下载入口。

### 15.2 第二阶段数据策略

第二阶段可以研究接入公开数据源，但需要单独处理数据版权、字段匹配、地理编码和更新频率问题。

可探索数据类型：

- 香港建筑物资料。
- 区域边界 GeoJSON。
- 土地用途资料。
- 建筑维修或强制验楼相关数据。
- 人口密度、楼宇密度或社会经济指标。

### 15.3 地理编码策略

如果原始 CSV 已有经纬度，系统直接使用 latitude 和 longitude。

如果只有地址，没有经纬度，v1.0 不自动地理编码，只提示缺少坐标。v2.0 再考虑接入地理编码服务。

原因：

- 地址格式不统一，自动地理编码容易产生错误点位。
- 免费服务有速率限制。
- 地理编码结果需要人工抽样核验。

## 16. 详细数据处理管线

系统数据处理建议采用固定管线，避免页面逻辑和数据逻辑混在一起。

```text
CSV 上传
  -> 字段读取
  -> 字段映射
  -> 类型转换
  -> 异常值检查
  -> 数据清洗
  -> 楼龄计算
  -> 楼龄分组
  -> 风险评分
  -> 风险等级
  -> 用户筛选
  -> 指标统计
  -> 图表 / 地图 / AI 摘要 / 报告
```

### 16.1 字段映射规则

系统可以内置常见别名：

| 标准字段 | 可识别别名 |
| --- | --- |
| building_id | id, bldg_id, building_code |
| building_name | name, bldg_name, estate_name |
| district | area, region, district_name |
| building_use | use, usage, property_type |
| completion_year | year, built_year, year_built, completion |
| latitude | lat, y |
| longitude | lon, lng, x |

如果字段无法自动识别，v1.0 应给出明确报错提示；人工字段映射界面可作为增强功能加入。

### 16.2 类型转换规则

- completion_year 转为整数。
- latitude 和 longitude 转为浮点数。
- district 和 building_use 转为字符串。
- 空字符串、`N/A`、`NA`、`null`、`-` 统一视为缺失。

### 16.3 异常数据处理规则

| 异常类型 | 处理方式 |
| --- | --- |
| 缺少 completion_year | 保留记录，但不计算楼龄和风险 |
| completion_year 大于当前年份 | 标为异常，不进入统计 |
| completion_year 小于 1800 | 标为异常，不进入统计 |
| 缺少经纬度 | 参与统计，不进入点位地图 |
| 经纬度不在香港范围 | 参与统计，但地图点位标为异常或过滤 |
| 缺少 district | 填 Unknown |
| 缺少 building_use | 填 Unknown |

### 16.4 数据质量输出

页面和报告都需要展示数据质量摘要：

- 原始记录数。
- 有效记录数。
- 被排除记录数。
- 缺少年份记录数。
- 缺少坐标记录数。
- 异常坐标记录数。
- 未知区域记录数。
- 未知用途记录数。

## 17. 风险模型深化

### 17.1 模型定位

风险模型是用于优先级筛查的规则模型，不是预测模型，也不是结构安全评估模型。

它回答的问题是：

- 哪些建筑因为楼龄和用途组合更值得优先关注？
- 哪些区域存在高龄建筑集中现象？
- 当前数据是否显示维护压力可能集中在某些区域或用途？

它不回答的问题是：

- 某栋楼是否存在结构危险。
- 某栋楼是否需要立即维修。
- 某栋楼的真实检测等级。

### 17.2 推荐风险因子

v1.0 使用最少字段也能运行：

- building_age。
- building_use。
- last_major_repair_year，如有。

P1 可加入：

- floor_count。
- gross_floor_area。
- inspection_status。
- occupancy_level。
- heritage_status。

### 17.3 风险评分输出解释

每条建筑除了 risk_score 和 risk_level，还建议输出 risk_reason。

示例：

```text
楼龄 55 年，属于 41-60 年老化关注区间；建筑用途为 Industrial，用途风险加权较高；缺少最近维修年份，因此维护信息不完整。
```

这样用户点击地图点位时，不只看到一个分数，也能知道分数为什么高。

### 17.4 风险等级颜色规范

| 风险等级 | 颜色 | 含义 |
| --- | --- | --- |
| Low | 绿色 | 常规关注 |
| Medium | 黄色 | 中等关注 |
| High | 橙色 | 优先关注 |
| Critical | 红色 | 重点关注 |

地图、图表、报告中的颜色含义必须保持一致。

## 18. 页面与交互深化

### 18.1 页面信息架构

Streamlit 页面建议使用如下顺序：

1. 顶部标题区。
2. 数据上传区。
3. 数据质量提示区。
4. 侧边栏筛选区。
5. 核心指标区。
6. 地图区。
7. 图表分析区。
8. AI 摘要区。
9. 报告导出区。

### 18.2 顶部标题区

需要包含：

- 系统名称。
- 当前数据来源或文件名。
- 当前分析记录数。

不需要做营销式大标题，页面应更像研究工具或分析仪表板。

### 18.3 数据上传区

要求：

- 支持 `.csv`。
- 显示上传文件名。
- 上传后展示前 5 行预览。
- 读取失败时提示编码或格式问题。

推荐支持 UTF-8 和 UTF-8-SIG。如果读取失败，再尝试 Big5 或 GBK。

### 18.4 筛选区

筛选区放在左侧 sidebar：

- district 多选。
- building_use 多选。
- age_band 多选。
- risk_level 多选。
- 重置筛选按钮。

筛选默认值为全部。

### 18.5 指标卡片区

建议 4 到 6 个指标卡片：

- Total Buildings。
- Average Age。
- Median Age。
- 40+ Years Share。
- 60+ Years Share。
- High / Critical Share。

每个指标卡片需要显示数值和单位，例如 `42.5 years`、`38.2%`。

### 18.6 地图区

地图默认中心点设置为香港：

```text
latitude: 22.3193
longitude: 114.1694
zoom: 11
```

点位 tooltip 字段：

- building_name。
- district。
- building_use。
- building_age。
- risk_level。
- risk_score。

点位太多时，P1 可以加入 marker clustering。

### 18.7 图表区

图表建议使用 tabs：

- Age Structure。
- Risk Distribution。
- District Ranking。
- Use Type Analysis。
- Data Quality。

这样页面不会过长，也方便演示。

### 18.8 AI 摘要区

AI 摘要区需要包含：

- 语言选择：中文 / English。
- 生成按钮。
- 当前摘要生成时间。
- 摘要文本框。
- 复制或下载按钮。

如果没有设置 API Key，显示：

```text
当前未配置 LLM API Key，系统使用规则模板生成摘要。
```

## 19. AI 摘要质量控制

### 19.1 输入控制

不要把全部原始 CSV 行直接发给 LLM。只发送聚合统计和少量 top records。

建议发送：

```json
{
  "filters": {},
  "summary_metrics": {},
  "risk_distribution": {},
  "age_band_distribution": {},
  "top_high_risk_districts": [],
  "top_high_risk_use_types": [],
  "data_quality": {}
}
```

### 19.2 输出控制

AI 输出必须遵守：

- 不编造不存在的数据。
- 不声称进行了现场检测。
- 不给出法律或工程安全定论。
- 不建议立即拆除或强制维修，除非用户提供真实检测依据。
- 明确说明数据局限。

### 19.3 摘要长度

v1.0 默认生成 400 到 700 字中文摘要，或者 300 到 500 词英文摘要。

报告模式可以稍长：

- 中文 800 到 1200 字。
- 英文 600 到 900 词。

### 19.4 模板摘要 fallback

无 API Key 时，用规则模板生成：

```text
当前筛选范围内共有 {total_buildings} 栋建筑，平均楼龄为 {average_age} 年。
其中 40 年以上建筑占比为 {share_40_plus}，60 年以上建筑占比为 {share_60_plus}。
高风险及重点关注建筑占比为 {high_critical_share}。
高风险建筑主要集中在 {top_districts}，用途上主要集中在 {top_use_types}。
本结果基于楼龄、用途和维修记录完整度进行初步筛查，不等同于正式结构安全评估。
```

## 20. 报告模板深化

导出报告建议采用固定结构，保证每次输出都可比较。

```text
# 香港建筑资产年龄与维护风险分析报告

## 1. 数据范围
- 文件名
- 分析时间
- 筛选条件
- 有效记录数

## 2. 核心指标
- 建筑总数
- 平均楼龄
- 中位楼龄
- 40 年以上建筑占比
- 60 年以上建筑占比
- 高风险建筑占比

## 3. 楼龄结构
- 楼龄区间分布
- 关键观察

## 4. 风险分层结果
- 风险等级分布
- 高风险区域
- 高风险用途

## 5. 地图与空间分布
- 地图说明
- 高风险点位集中情况

## 6. AI 风险摘要与维护建议
- Overall Risk Summary
- Key Observations
- Maintenance Priorities
- Data Limitations

## 7. 方法说明
- 风险评分规则
- 数据清洗规则
- 模型限制

## 8. 附录
- 字段说明
- 异常数据摘要
```

## 21. 工程实现细节

### 21.1 requirements.txt

建议初始依赖：

```text
pandas
numpy
streamlit
plotly
folium
streamlit-folium
python-dotenv
openai
```

如果第二阶段需要 GeoJSON 区域分析，再加入：

```text
geopandas
shapely
pyproj
```

### 21.2 环境变量

`.env.example` 建议包含：

```text
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
APP_DEFAULT_LANGUAGE=zh
```

如果使用不同 LLM 服务，需要把 LLM 调用封装在 `llm_summary.py`，不要散落在页面代码里。

### 21.3 日志与错误处理

v1.0 至少需要处理：

- CSV 读取失败。
- 缺少必需字段。
- 数据筛选后为空。
- LLM 调用失败。
- 报告生成失败。

错误提示要面向用户，而不是直接显示 Python traceback。

## 22. 测试用例

### 22.1 数据读取测试

| 测试场景 | 预期结果 |
| --- | --- |
| 上传标准 CSV | 成功读取并展示预览 |
| 上传缺少 completion_year 的 CSV | 提示缺少关键字段 |
| 上传空 CSV | 提示文件为空 |
| 上传乱码 CSV | 尝试备用编码或提示编码错误 |

### 22.2 清洗测试

| 测试场景 | 预期结果 |
| --- | --- |
| completion_year 为 1970 | 正确计算楼龄 |
| completion_year 大于当前年份 | 标为异常 |
| district 为空 | 填 Unknown |
| building_use 大小写混乱 | 标准化为统一分类 |

### 22.3 风险评分测试

| 测试场景 | 预期结果 |
| --- | --- |
| 新建筑 Residential | Low 或 Medium |
| 55 年 Industrial | High |
| 70 年 Mixed 且无维修记录 | Critical |
| 缺少年份 | 不计算风险，并提示 |

### 22.4 页面交互测试

| 测试场景 | 预期结果 |
| --- | --- |
| 选择一个 district | 所有指标和图表同步变化 |
| 风险筛选选 High | 地图只显示 High 建筑 |
| 筛选结果为空 | 页面提示无数据 |
| 点击生成摘要 | 返回结构化文本 |

### 22.5 报告导出测试

| 测试场景 | 预期结果 |
| --- | --- |
| 有 AI 摘要时导出 | 报告包含摘要 |
| 无 AI 摘要时导出 | 报告提示未生成 AI 摘要或使用模板摘要 |
| 筛选后导出 | 报告记录当前筛选条件 |

## 23. 开发排期建议

如果按个人开发或小组开发推进，可以分为 5 个工作包。

| 工作包 | 预计时间 | 产出 |
| --- | --- | --- |
| WP1 数据样例和读取 | 0.5-1 天 | sample CSV、读取函数、字段检查 |
| WP2 清洗和风险模型 | 1 天 | cleaning.py、risk_model.py、单元测试 |
| WP3 Streamlit 页面 | 1-2 天 | app.py、筛选、指标、图表 |
| WP4 地图和 AI 摘要 | 1-2 天 | folium 地图、llm_summary.py |
| WP5 报告导出和整理 | 0.5-1 天 | report_generator.py、README、演示流程 |

第一轮可运行产品基线预计 4 到 7 天可以完成，完整 v1.0 成品需要继续完成监管信号、AI 报告、验收和交付打磨。

## 24. 项目交付物

v1.0 交付时建议包含：

- 可运行代码。
- `requirements.txt`。
- `.env.example`。
- 样例 CSV。
- README 使用说明。
- 风险评分方法说明。
- 至少 1 份导出的示例报告。
- 截图或短视频演示。

README 至少说明：

- 如何安装依赖。
- 如何启动系统。
- CSV 需要哪些字段。
- 如何配置 API Key。
- 风险评分的限制。

## 25. 关键风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 真实数据字段不统一 | 系统无法直接读取 | 做字段映射和清洗提示 |
| 坐标缺失较多 | 地图展示不完整 | 地图与统计分开，缺坐标仍参与统计 |
| 用户误解风险评分 | 可能把 dashboard 当工程检测结论 | 页面和报告反复声明模型限制 |
| LLM 生成内容夸大 | 报告可信度下降 | 使用结构化 prompt 和限制语句 |
| 依赖 geopandas 安装困难 | 本地运行门槛高 | v1.0 优先避免强依赖 geopandas |
| 数据量过大导致页面卡顿 | 体验差 | 先限制上传规模，P1 再优化 |

## 26. 最终落地路线图

建议按以下路线执行：

1. 用官方数据或 sample 数据完成端到端产品基线。
2. 把清洗、评分、统计、可视化拆成独立模块。
3. 加入 AI 摘要和 fallback 模板摘要。
4. 导出 Markdown 报告。
5. 用真实数据测试字段兼容和异常处理。
6. 根据演示反馈调整风险模型权重。
7. 再决定是否进入 v2.0：PDF 高级排版、数据库、用户系统和公网部署。

最终判断标准不是代码功能有多少，而是系统是否能稳定回答三个问题：

- 香港建筑资产老化程度如何？
- 哪些区域和用途更值得优先关注？
- 如何把分析结果转化成清晰、谨慎、可用于汇报的维护建议？

## 27. 真实数据调研与落地数据源

本项目后续不应只依赖用户手工整理的 CSV。为了做成可以真实使用的 dashboard，需要在前期明确官方数据源、字段口径、更新频率和数据接入方式。

### 27.1 主数据源：Building information and age records

主数据源建议采用香港屋宇署通过 DATA.GOV.HK / CSDI 发布的 `Building information and age records`。

官方说明：

- 数据提供机构：Buildings Department。
- 数据格式：CSV，实际下载为 zip 包，解压后得到 CSV。
- 更新频率：Yearly。
- 内容：私人楼宇资料，包括地址、建筑类型、建筑用途、block ID、occupation permit date、occupation permit number。
- 官方数据页：https://data.gov.hk/en-data/dataset/hk-bd-opendata-building-information
- 当前资源下载入口：https://static.csdi.gov.hk/csdi-webpage/download/0e55c533715b5da3ae0ca6e6024e90b4/csv

本次预研下载样本显示，解压后的 CSV 包含约 51,037 条记录。这个量级适合直接用 pandas / DuckDB / Parquet 做本地 dashboard，不需要一开始上复杂数据库。

### 27.2 主数据字段映射

官方字段不是用户友好的业务字段名，因此系统内部必须做字段标准化。

| 官方字段 | 建议内部字段 | 含义 | 处理方式 |
| --- | --- | --- | --- |
| OBJECTID | source_object_id | 官方对象编号 | 保留 |
| ADDRESS_E | address_en | 英文地址 | 用于展示和地址匹配 |
| ADDRESS_C | address_tc | 中文地址 | 用于展示 |
| SEARCH1_E | district_en | 18 区英文名 | 作为主要区域字段 |
| SEARCH1_C | district_tc | 18 区中文名 | 用于中文界面 |
| SEARCH2_E | territory_en | Hong Kong / Kowloon / New Territories | 用于宏观区域筛选 |
| NSEARCH1_E | block_id | block ID | 作为候选建筑识别字段 |
| NSEARCH2_E | occupation_permit_no | Occupation Permit number | 作为建筑年代依据之一 |
| NSEARCH3_E | occupation_permit_date | Occupation Permit date | 用于计算楼龄 |
| NSEARCH4_E | building_type | Tower / Podium | 建筑类型 |
| NSEARCH5_E | building_usage | Residential/Composite / Office/Commercial / Industrial / Others | 建筑用途 |
| LATITUDE | latitude | 纬度 | 地图点位 |
| LONGITUDE | longitude | 经度 | 地图点位 |
| GeometryLongitude | geometry_longitude | 几何经度 | 优先用于空间计算 |
| GeometryLatitude | geometry_latitude | 几何纬度 | 优先用于空间计算 |

内部系统不应继续把 `completion_year` 作为唯一必需字段。对官方数据，应从 `occupation_permit_date` 提取年份，再计算楼龄。

### 27.3 官方用途分类基线

根据预研样本，官方建筑用途字段主要包括：

- Residential/Composite。
- Office/Commercial。
- Industrial。
- Others。

因此 v1.0 的用途筛选应优先使用官方这 4 类，而不是一开始设计过多自定义类别。后续如需要更细的用途分类，可以在 `Others` 内进行二次标签化。

### 27.4 官方建筑类型基线

根据预研样本，官方建筑类型主要包括：

- Tower。
- Podium。

dashboard 中应加入 `building_type` 筛选和统计，因为 Tower 与 Podium 的楼龄分布、维护责任和空间表现可能不同。

### 27.5 区界数据源：District boundary

区域边界建议采用 Home Affairs Department 发布的 18 区边界数据。

官方说明：

- 数据提供机构：Home Affairs Department。
- 数据格式：JSON / GML / API。
- 内容：18 区中英文名称和边界坐标。
- 官方数据页：https://data.gov.hk/en-data/dataset/hk-had-json1-hong-kong-administrative-boundaries
- JSON 下载：https://www.had.gov.hk/psi/hong-kong-administrative-boundaries/hksar_18_district_boundary.json

该数据用于：

- 18 区 choropleth 地图。
- 点位落区校验。
- district 字段与空间边界的一致性检查。
- 区域级风险指数计算。

### 27.6 监管事件数据源：MBIS / MWIS 法定通知

为了让 dashboard 更接近真实维护管理场景，需要把楼龄分析与监管通知数据结合。

建议接入以下数据：

| 数据集 | 用途 | 更新频率 | 建议阶段 |
| --- | --- | --- | --- |
| Statutory Notices issued on prescribed inspection/repair for buildings | MBIS 楼宇订明检验/修葺通知 | Monthly | P1 |
| Statutory Notices complied with/withdrawn/superseded on prescribed inspection/repair for buildings | MBIS 通知完成/撤回/取代 | Monthly | P1 |
| Statutory Notices issued on prescribed inspection/repair for windows | MWIS 窗户订明检验/修葺通知 | Monthly | P2 |
| Statutory Notices complied with/withdrawn/superseded on prescribed inspection/repair for windows | MWIS 通知完成/撤回/取代 | Monthly | P2 |

MBIS issued CSV 入口示例：

```text
https://portal.csdi.gov.hk/server/services/common/bd_rcd_1631168029910_44937/MapServer/WFSServer?service=wfs&request=GetFeature&typename=BDMBIS&outputFormat=CSV
```

MWIS issued CSV 入口示例：

```text
https://portal.csdi.gov.hk/server/services/common/bd_rcd_1631168094734_86276/MapServer/WFSServer?service=wfs&request=GetFeature&typename=BDMWIS&outputFormat=CSV
```

MBIS / MWIS 字段中通常包含：

- NAME_EN / NAME_TC：建筑或记录名称。
- ADDRESS_EN / ADDRESS_TC：地址。
- NSEARCH01_E：通知数量或相关数量字段。
- NSEARCH02_E：通知编号或相关识别字段。
- LATITUDE / LONGITUDE：点位。
- LASTUPDATE：数据更新时间。

这些数据不应简单等同于“建筑危险”，而应作为监管压力和维护事件的 proxy。

## 28. 数据接入架构深化

### 28.1 数据接入模式

dashboard 需要支持三种数据接入方式：

| 模式 | 使用场景 | 实现优先级 |
| --- | --- | --- |
| 官方在线拉取 | 使用最新公开数据 | P1 |
| 本地缓存读取 | 网络不可用时仍能演示 | P0 |
| 用户上传 CSV | 支持自有研究数据 | P0 |

页面上应显示当前数据来源：

- Official source。
- Cached official source。
- User uploaded file。
- Sample dataset。

### 28.2 数据目录结构升级

建议将项目目录升级为可复现实验结构：

```text
project/
  app.py
  requirements.txt
  .env.example
  README.md
  需求文档.md
  data/
    raw/
      bdbiar/
      district_boundary/
      mbis/
      mwis/
    processed/
      buildings.parquet
      district_metrics.parquet
      regulatory_notices.parquet
    sample/
      sample_buildings.csv
    cache/
      source_manifest.json
  outputs/
    reports/
    figures/
    exports/
  src/
    data_sources/
      fetch_bdbiar.py
      fetch_boundaries.py
      fetch_mbis.py
    pipeline/
      normalize_buildings.py
      spatial_join.py
      build_metrics.py
    dashboard/
      components.py
      charts.py
      maps.py
    models/
      risk_rules.py
      district_index.py
    ai/
      prompt_builder.py
      llm_client.py
      template_summary.py
    reports/
      markdown_report.py
      html_report.py
    utils/
      config.py
      logging.py
```

### 28.3 数据源 manifest

每次下载官方数据后，系统应写入 `source_manifest.json`：

```json
{
  "bdbiar": {
    "source_name": "Building information and age records",
    "provider": "Buildings Department",
    "url": "https://static.csdi.gov.hk/csdi-webpage/download/0e55c533715b5da3ae0ca6e6024e90b4/csv",
    "downloaded_at": "2026-06-06T13:00:00+08:00",
    "raw_file": "data/raw/bdbiar/BDBIAR_gdb_2026_02_23_BDBIAR_converted.csv",
    "record_count": 51037
  }
}
```

报告导出时必须带上数据版本信息，保证后续硕士或博士研究可以复现。

### 28.4 推荐数据存储格式

开发阶段可以使用 CSV，但 dashboard 运行阶段建议使用 Parquet：

- CSV 作为原始留档。
- Parquet 作为处理后缓存。
- DuckDB 用于快速聚合查询。

建议新增依赖：

```text
requests
pyarrow
duckdb
```

## 29. 研究级指标体系

dashboard 不应只展示几个简单图表。它需要围绕“建筑资产老化与维护优先级”形成指标体系。

### 29.1 建筑个体级指标

| 指标 | 计算方式 | 用途 |
| --- | --- | --- |
| building_age | 当前年份 - occupation_permit_year | 楼龄 |
| age_band | 按楼龄分组 | 可视化和筛选 |
| is_30_plus | building_age >= 30 | 对齐 MBIS 年龄门槛 |
| is_40_plus | building_age >= 40 | 老化关注 |
| is_50_plus | building_age >= 50 | 高龄资产 |
| is_60_plus | building_age >= 60 | 重点关注 |
| usage_group | 官方用途标准化 | 用途分组 |
| type_group | Tower / Podium | 类型分组 |
| has_valid_coordinate | 经纬度是否有效 | 地图可用性 |
| has_mbis_notice | 是否匹配 MBIS 通知 | 监管事件标记 |
| has_mwis_notice | 是否匹配 MWIS 通知 | 窗户维护事件标记 |
| risk_score | 规则评分 | 初步排序 |
| risk_level | Low / Medium / High / Critical | 用户解释 |
| risk_reason | 文字解释 | AI 和详情页使用 |

### 29.2 区域级指标

| 指标 | 计算方式 | 用途 |
| --- | --- | --- |
| total_buildings | 区域建筑数 | 规模 |
| median_age | 区域楼龄中位数 | 老化水平 |
| p75_age | 区域楼龄 75 分位 | 高龄尾部 |
| share_30_plus | 30 年以上占比 | MBIS 相关 |
| share_40_plus | 40 年以上占比 | 老化压力 |
| share_60_plus | 60 年以上占比 | 高龄压力 |
| high_critical_share | High + Critical 占比 | 风险集中度 |
| mbis_notice_rate | MBIS 通知数 / 建筑数 * 1000 | 监管压力 |
| mwis_notice_rate | MWIS 通知数 / 建筑数 * 1000 | 窗户维护压力 |
| data_completeness | 关键字段完整率 | 数据可信度 |
| district_priority_index | 综合优先级指数 | 区域排序 |

### 29.3 区域综合优先级指数

建议 P1 版本引入 `district_priority_index`，用于 dashboard 中对 18 区排序。

初始公式：

```text
district_priority_index =
  0.30 * normalized_median_age
  + 0.25 * normalized_share_50_plus
  + 0.20 * normalized_high_critical_share
  + 0.15 * normalized_mbis_notice_rate
  + 0.10 * normalized_industrial_or_commercial_share
```

说明：

- 所有子指标先做 0-100 标准化。
- MBIS 通知数据缺失时，该项权重按比例重新分配给楼龄和高风险占比。
- 该指数只用于研究和管理优先级排序，不作为工程安全评级。

### 29.4 Dashboard 核心问题

dashboard 需要稳定回答以下研究问题：

1. 香港私人建筑资产的年龄结构如何？
2. 哪些区域存在 30 年、40 年、50 年、60 年以上建筑集中？
3. 哪些用途类别更容易形成高楼龄或高风险集中？
4. 楼龄高的区域是否也有更高的 MBIS / MWIS 通知密度？
5. 如果只能优先关注若干区域，应如何排序？
6. 当前数据有哪些缺口会影响维护风险判断？

## 30. 风险模型落地升级

### 30.1 风险模型分层

建议把风险模型拆成三层，而不是一个单一分数。

| 层级 | 名称 | 输入 | 输出 |
| --- | --- | --- | --- |
| Layer 1 | Age Screening | 楼龄、用途、类型 | age_risk_score |
| Layer 2 | Regulatory Signal | MBIS/MWIS 通知 | regulatory_signal_score |
| Layer 3 | Data Confidence | 字段完整性、坐标质量、匹配质量 | confidence_score |

最终展示：

```text
final_risk_score = age_risk_score + regulatory_signal_score
```

同时单独展示：

```text
confidence_score
```

避免用户把数据缺失导致的不确定性误认为真实低风险。

### 30.2 个体风险评分建议

Age Screening：

| 条件 | 分数 |
| --- | --- |
| 0-19 年 | 10 |
| 20-29 年 | 25 |
| 30-39 年 | 45 |
| 40-49 年 | 60 |
| 50-59 年 | 75 |
| 60 年以上 | 85 |

用途加权：

| 用途 | 加分 |
| --- | --- |
| Residential/Composite | +8 |
| Office/Commercial | +10 |
| Industrial | +15 |
| Others | +5 |

类型加权：

| 类型 | 加分 |
| --- | --- |
| Tower | +5 |
| Podium | +3 |

监管事件加权：

| 条件 | 加分 |
| --- | --- |
| 匹配 MBIS issued notice | +15 |
| 匹配 MWIS issued notice | +8 |
| 已 complied / withdrawn / superseded | -5 至 -10，视字段可靠性决定 |

最终分数限制在 0 到 100。

### 30.3 风险等级

| risk_score | risk_level | 含义 |
| --- | --- | --- |
| 0-30 | Low | 常规关注 |
| 31-55 | Medium | 中等关注 |
| 56-75 | High | 优先关注 |
| 76-100 | Critical | 重点关注 |

### 30.4 风险解释字段

每个建筑需要生成结构化解释：

```json
{
  "risk_drivers": [
    "Occupation Permit date indicates building age above 50 years",
    "Usage group is Industrial",
    "Matched MBIS notice record within spatial/address tolerance"
  ],
  "data_cautions": [
    "Risk score is based on public records and rule-based screening",
    "No structural inspection result is included"
  ]
}
```

AI 摘要只能引用这些结构化解释，不允许自行创造原因。

## 31. MBIS / MWIS 匹配策略

### 31.1 为什么不能直接按 ID 匹配

主建筑年龄表与 MBIS/MWIS 通知表不一定共享稳定的统一 building_id。因此不能假设 `OBJECTID`、`NAME_EN` 或 `NSEARCH` 字段可以直接一对一连接。

### 31.2 推荐匹配方法

匹配应分三步：

1. 地址标准化匹配：
   - 转大写。
   - 移除多余空格和标点。
   - 统一 `RD` / `ROAD`、`ST` / `STREET` 等常见缩写。
2. 空间近邻匹配：
   - 经纬度转换为 GeoDataFrame。
   - 在 20m、50m、100m 三个半径内测试最近邻。
   - 默认采用 50m 半径，并记录匹配距离。
3. 置信度标记：
   - exact_address_match。
   - fuzzy_address_match。
   - spatial_match_20m。
   - spatial_match_50m。
   - unmatched。

### 31.3 匹配结果使用原则

- exact_address_match 和 spatial_match_20m 可用于个体风险加权。
- fuzzy_address_match 和 spatial_match_50m 可用于区域统计，但个体层面需要标记不确定。
- unmatched 不代表没有通知，只代表当前数据无法匹配。

报告中必须说明匹配方法和匹配率。

## 32. Dashboard 产品结构深化

### 32.1 页面采用多 Tab 分析工作台

建议 Streamlit 使用 tabs 或 sidebar navigation。

| 页面 | 目标 | 主要内容 |
| --- | --- | --- |
| 1. Data Intake | 数据接入和质量检查 | 官方拉取、上传、缓存、字段映射、质量报告 |
| 2. Portfolio Overview | 全局概览 | 建筑总量、年龄结构、用途结构、风险结构 |
| 3. Spatial Dashboard | 空间分布 | 点位地图、18 区 choropleth、监管通知图层 |
| 4. District Benchmark | 区域比较 | 18 区排名、优先级指数、区域详情 |
| 5. Use & Typology | 用途和类型分析 | 用途/类型与楼龄、风险、通知的交叉分析 |
| 6. Regulatory Signals | MBIS/MWIS 专题 | 通知分布、通知密度、匹配率、趋势 |
| 7. AI Report Studio | 报告生成 | 摘要生成、方法说明、导出 |
| 8. Method & QA | 方法和质量 | 模型规则、数据源版本、异常记录、限制 |

### 32.2 Portfolio Overview

该页用于回答“整体情况如何”。

必备组件：

- 核心指标卡：
  - Total private building records。
  - Median building age。
  - 30+ share。
  - 50+ share。
  - High/Critical share。
  - MBIS notice matched count。
- 楼龄分布直方图。
- 用途分类饼图或条形图。
- 风险等级堆叠条形图。
- Top 5 ageing districts。
- Top 5 high-priority districts。

### 32.3 Spatial Dashboard

该页用于回答“风险在哪里”。

地图图层：

- Building points by risk level。
- District choropleth by median_age。
- District choropleth by share_50_plus。
- District choropleth by district_priority_index。
- MBIS notice points。
- MWIS notice points。

交互要求：

- 图层开关。
- district hover tooltip。
- 建筑点位点击详情。
- 风险等级图例。
- 数据量过大时支持聚合或抽样渲染。

### 32.4 District Benchmark

该页用于回答“18 区如何排序”。

建议组件：

- 18 区 priority index 排名表。
- 可排序指标列。
- 区域详情面板。
- 区域雷达图或条形对比图。
- 区域高龄建筑用途结构。
- 区域 MBIS/MWIS 通知密度。

排名表字段：

| 字段 | 说明 |
| --- | --- |
| district | 区名 |
| total_buildings | 建筑数 |
| median_age | 中位楼龄 |
| share_50_plus | 50 年以上占比 |
| high_critical_share | 高风险占比 |
| mbis_notice_rate | MBIS 通知密度 |
| district_priority_index | 综合优先级 |
| data_quality_score | 数据质量分 |

### 32.5 AI Report Studio

该页用于把 dashboard 当前状态转成研究报告。

用户可以选择：

- 报告语言：中文 / English。
- 报告粒度：全港 / 区域 / 用途 / 自定义筛选。
- 报告类型：
  - Executive Summary。
  - Research Note。
  - Maintenance Priority Brief。
  - Data Quality Memo。
- 是否包含方法说明。
- 是否包含数据源版本。

输出不应只是 AI 文本，还应包含：

- 当前筛选条件。
- 指标 JSON。
- 图表占位或导出的图表路径。
- 模型版本。
- 数据版本。

## 33. 研究与验证设计

为了支撑硕士或博士方向，dashboard 需要内置可验证、可复现、可解释的研究设计。

### 33.1 初始研究问题

可将项目研究问题定义为：

1. 香港私人建筑资产楼龄是否在空间上呈现明显区域聚集？
2. 不同用途建筑的老化程度是否存在显著差异？
3. 楼龄压力较高的区域是否也更容易出现 MBIS / MWIS 通知？
4. 规则型风险指数能否较好识别监管通知较集中的区域？
5. LLM 能否在不读取原始敏感数据的情况下，基于结构化指标生成可靠的维护优先级摘要？

### 33.2 可验证假设

| 假设 | 验证方法 |
| --- | --- |
| H1：高龄建筑在部分旧城区显著集中 | 18 区 share_50_plus / share_60_plus 排名 |
| H2：Industrial 和 Office/Commercial 的高龄风险更集中 | 用途-楼龄交叉表和风险分布 |
| H3：高龄占比高的区域，MBIS 通知密度也较高 | district-level correlation |
| H4：加入 MBIS 信号后，区域优先级排序更接近监管事件分布 | 对比 age-only index 与 combined index |
| H5：结构化 prompt 能减少 AI 摘要中的无依据推断 | 人工审查摘要中的 unsupported claim |

### 33.3 验证指标

| 验证对象 | 指标 |
| --- | --- |
| 数据处理 | 有效记录率、坐标有效率、楼龄计算成功率 |
| 空间匹配 | MBIS 匹配率、20m/50m/100m 匹配分布 |
| 风险模型 | district ranking 与 MBIS notice rate 的相关性 |
| Dashboard | 页面加载时间、筛选响应时间、导出成功率 |
| AI 摘要 | 数据一致性、无编造率、限制说明完整率 |

### 33.4 最低验证流程

v1.0 交付前必须做以下验证：

1. 随机抽查 30 条建筑记录，核对地址、区名、用途、OP 日期、经纬度是否合理。
2. 检查 18 区边界空间连接结果，确认 district 字段与 polygon 结果冲突率。
3. 对 top 20 Critical 建筑进行人工快速审查，判断风险解释是否合理。
4. 对 18 区 priority ranking 做 sanity check，确认排序不是由数据缺失造成。
5. 对 10 份 AI 摘要做人工审核，标记是否出现编造、过度结论或遗漏限制。

## 34. LLM 使用边界升级

### 34.1 LLM 在系统中的角色

LLM 只负责解释和报告生成，不负责直接判断结构风险。

允许 LLM 做：

- 汇总 dashboard 指标。
- 解释区域和用途差异。
- 生成维护优先级建议。
- 说明数据局限。
- 生成面向研究汇报的文字。

不允许 LLM 做：

- 判断某栋楼是否危险。
- 编造检测记录。
- 直接建议拆除或强制维修。
- 把监管通知解释成结构缺陷事实。
- 在没有数据的情况下推断原因。

### 34.2 Prompt 输入必须结构化

LLM 输入建议固定为：

```json
{
  "analysis_scope": {
    "geography": "Hong Kong / selected district",
    "filters": {},
    "data_version": {},
    "model_version": "risk_rules_v0.2"
  },
  "metrics": {
    "portfolio": {},
    "age_distribution": {},
    "risk_distribution": {},
    "district_ranking": [],
    "usage_analysis": [],
    "regulatory_signals": {}
  },
  "quality": {
    "record_count": 0,
    "missing_values": {},
    "coordinate_validity": {},
    "matching_confidence": {}
  },
  "required_disclaimers": [
    "This is not a structural inspection.",
    "Risk scores are rule-based screening indicators.",
    "Regulatory notices are used as public-record signals, not direct proof of structural defect."
  ]
}
```

### 34.3 AI 输出审查规则

系统应自动检查 AI 输出：

- 是否包含 “not a structural inspection” 或对应中文说明。
- 是否提到了数据源和数据局限。
- 是否出现 “dangerous building” 等过度定性词。
- 是否引用了输入中不存在的区域、数字或用途。

如果输出不合格，系统应提示重新生成或降级为模板摘要。

## 35. 开发任务批量化清单

为了后续可以大步推进，建议把开发任务拆成可直接执行的 issue。

### 35.1 Data Pipeline Issues

| 编号 | 任务 | 产出 |
| --- | --- | --- |
| DP-01 | 实现官方 building age zip 下载与解压 | `fetch_bdbiar.py` |
| DP-02 | 实现官方字段标准化 | `normalize_buildings.py` |
| DP-03 | 从 occupation_permit_date 计算楼龄 | `building_age` 字段 |
| DP-04 | 生成 Parquet 缓存 | `buildings.parquet` |
| DP-05 | 接入 18 区 boundary JSON | `district_boundary.geojson` |
| DP-06 | 实现点位落区空间校验 | `district_spatial_check.parquet` |
| DP-07 | 接入 MBIS issued CSV | `mbis_issued.parquet` |
| DP-08 | 实现 MBIS 与主表匹配 | `mbis_match_result.parquet` |
| DP-09 | 生成 source manifest | `source_manifest.json` |

### 35.2 Analytics Issues

| 编号 | 任务 | 产出 |
| --- | --- | --- |
| AN-01 | 个体风险评分 v0.2 | `risk_rules.py` |
| AN-02 | 区域指标聚合 | `district_metrics.parquet` |
| AN-03 | 区域优先级指数 | `district_index.py` |
| AN-04 | 用途-楼龄-风险交叉分析 | 图表数据 |
| AN-05 | MBIS 通知密度分析 | 监管信号指标 |
| AN-06 | 数据质量指标体系 | QA 面板数据 |

### 35.3 Dashboard Issues

| 编号 | 任务 | 产出 |
| --- | --- | --- |
| UI-01 | Data Intake 页面 | 数据源与质量检查 |
| UI-02 | Portfolio Overview 页面 | 全局指标和图表 |
| UI-03 | Spatial Dashboard 页面 | 多图层地图 |
| UI-04 | District Benchmark 页面 | 18 区排序 |
| UI-05 | Use & Typology 页面 | 用途和类型分析 |
| UI-06 | Regulatory Signals 页面 | MBIS/MWIS 专题 |
| UI-07 | AI Report Studio 页面 | 摘要和报告生成 |
| UI-08 | Method & QA 页面 | 方法、版本和异常 |

### 35.4 AI & Report Issues

| 编号 | 任务 | 产出 |
| --- | --- | --- |
| AI-01 | 结构化指标 JSON 生成 | `prompt_builder.py` |
| AI-02 | LLM 调用封装 | `llm_client.py` |
| AI-03 | 模板摘要 fallback | `template_summary.py` |
| AI-04 | AI 输出安全审查 | `validate_summary.py` |
| RP-01 | Markdown 报告导出 | `.md` 报告 |
| RP-02 | HTML 报告导出 | `.html` 报告 |
| RP-03 | 方法附录自动生成 | methodology section |

## 36. 阶段路线图升级

### 36.1 Phase 0：数据可行性验证

目标：确认官方数据能稳定读取、标准化和计算楼龄。

交付：

- 官方 building age 数据下载脚本。
- 解压和字段标准化脚本。
- 记录数、字段表、用途分类、区名分类统计。
- 初版数据质量报告。

完成标准：

- 能从官方 zip 生成 `buildings.parquet`。
- 90% 以上记录能计算楼龄。
- 经纬度字段能用于地图。

### 36.2 Phase 1：Dashboard 产品基线

目标：做出可以演示和使用的本地 dashboard。

交付：

- Data Intake。
- Portfolio Overview。
- Spatial Dashboard。
- District Benchmark。
- 风险评分 v0.2。
- Markdown 报告导出。
- 模板摘要 fallback。

完成标准：

- 本地 `streamlit run app.py` 可运行。
- 支持官方缓存和用户上传。
- 18 区、用途、楼龄、风险筛选可用。
- 主要图表和地图响应稳定。

### 36.3 Phase 2：监管信号增强

目标：把楼龄 dashboard 升级为维护优先级 dashboard。

交付：

- MBIS issued 数据接入。
- MBIS 匹配策略。
- Regulatory Signals 页面。
- district_priority_index。
- AI Report Studio。

完成标准：

- 至少能在区域层面展示 MBIS 通知密度。
- 能比较 age-only risk 和 combined risk。
- 报告中能说明监管信号和数据局限。

### 36.4 Phase 3：研究级验证与论文材料

目标：让 dashboard 支撑硕士论文或博士预研。

交付：

- 指标方法说明。
- 数据源版本说明。
- 区域排名验证。
- AI 摘要质量审查。
- 实验结果导出。
- 可复现实验脚本。

完成标准：

- 所有图表可导出。
- 所有指标可追溯到数据源和计算脚本。
- 能形成研究方法章节的基础材料。

## 37. 落地技术选择再判断

### 37.1 为什么仍然建议先用 Streamlit

当前数据量约 5 万条，且核心是分析、筛选、地图和报告生成。Streamlit 足够支撑研究级 dashboard 产品，开发速度快，适合交付 v1.0 local-first 成品。

但实现时不要把所有逻辑写进 `app.py`。必须保持模块化：

- 数据接入脚本独立。
- 指标计算独立。
- 风险模型独立。
- AI 摘要独立。
- dashboard 只负责调用和展示。

### 37.2 什么时候需要升级技术栈

如果出现以下情况，再考虑 FastAPI + React / Next.js：

- 需要多人登录。
- 需要数据库保存多个项目。
- 需要长期在线部署。
- 需要复杂权限。
- 需要更复杂地图性能。
- 需要团队协作开发前后端。

在此之前，Streamlit + DuckDB + Parquet 是更务实的路线。

## 38. 最终深化后的产品定位

本项目不应定位为普通“CSV 可视化小工具”，而应定位为：

```text
一个面向香港私人建筑资产老化研究与维护优先级分析的可复现 dashboard 产品。
```

它的核心价值不是单纯画图，而是把以下内容串成闭环：

- 官方建筑年龄数据。
- 官方地理边界数据。
- 官方 MBIS/MWIS 监管通知数据。
- 可解释风险规则。
- 区域优先级排序。
- 数据质量和模型限制。
- AI 辅助报告生成。
- 可复现的数据和方法记录。

## 39. 开发前冻结判断

从产品和研究方向看，当前需求深度已经足够进入 Phase 0 和 Phase 1 开发。

但如果后续要用批量 PR 或自动化 agent 大步推进，必须再冻结以下内容：

- 数据契约：每个处理后数据表必须有哪些字段。
- 命令入口：每个 pipeline 如何运行。
- PR 顺序：哪些任务必须先做，哪些可以并行。
- 质量门禁：每个 PR 合并前必须通过什么检查。
- 演示验收脚本：开发完成后如何确认 dashboard 真的可用。

因此本章之后的内容作为“开发执行规格”，用于把需求文档转成可批量实施的工程任务。

## 40. 数据契约

### 40.1 `buildings.parquet`

`buildings.parquet` 是 dashboard 的核心事实表。所有页面、指标和风险模型都应优先从该表读取。

必需字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| source_object_id | int / string | 官方 OBJECTID |
| source_dataset | string | 数据源名称 |
| address_en | string | 英文地址 |
| address_tc | string | 中文地址 |
| district_en | string | 18 区英文名 |
| district_tc | string | 18 区中文名 |
| territory_en | string | Hong Kong / Kowloon / New Territories |
| block_id | string | 官方 block ID |
| occupation_permit_no | string | Occupation Permit number |
| occupation_permit_date | date | Occupation Permit 日期 |
| occupation_permit_year | int | Occupation Permit 年份 |
| building_age | int | 当前年份 - occupation_permit_year |
| age_band | string | 楼龄分组 |
| building_type | string | Tower / Podium |
| building_usage | string | 官方用途 |
| usage_group | string | 标准化用途 |
| latitude | float | 纬度 |
| longitude | float | 经度 |
| has_valid_coordinate | bool | 坐标是否有效 |
| risk_score | float | 0-100 风险分 |
| risk_level | string | Low / Medium / High / Critical |
| risk_reason | string | 风险解释 |
| data_quality_flags | string / json | 数据质量标记 |

字段约束：

- `building_age` 不可为负数。
- `risk_score` 必须在 0 到 100。
- `risk_level` 只能是 `Low`、`Medium`、`High`、`Critical`、`Unknown`。
- 坐标无效时 `has_valid_coordinate=false`，但记录仍保留。
- 无法解析 OP 日期时 `building_age` 和 `risk_score` 可为空，但必须有 `data_quality_flags`。

### 40.2 `district_metrics.parquet`

区域指标表用于 District Benchmark 和 choropleth 地图。

必需字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| district_en | string | 18 区英文名 |
| district_tc | string | 18 区中文名 |
| total_buildings | int | 建筑记录数 |
| valid_age_count | int | 可计算楼龄记录数 |
| median_age | float | 中位楼龄 |
| mean_age | float | 平均楼龄 |
| p75_age | float | 75 分位楼龄 |
| share_30_plus | float | 30 年以上占比 |
| share_40_plus | float | 40 年以上占比 |
| share_50_plus | float | 50 年以上占比 |
| share_60_plus | float | 60 年以上占比 |
| high_critical_count | int | High + Critical 数量 |
| high_critical_share | float | High + Critical 占比 |
| mbis_notice_count | int | MBIS 通知匹配数 |
| mbis_notice_rate | float | 每 1000 栋建筑的 MBIS 通知数 |
| data_quality_score | float | 0-100 数据质量分 |
| district_priority_index | float | 0-100 区域优先级指数 |

### 40.3 `regulatory_notices.parquet`

监管通知表用于 MBIS/MWIS 专题页。

必需字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| notice_source | string | MBIS_issued / MBIS_complied / MWIS_issued / MWIS_complied |
| source_object_id | string | 原始对象编号 |
| notice_name_en | string | 通知记录名称 |
| address_en | string | 英文地址 |
| address_tc | string | 中文地址 |
| notice_count | int | 通知数量字段 |
| notice_ref | string | 通知编号或参考字段 |
| latitude | float | 纬度 |
| longitude | float | 经度 |
| last_update | datetime | 官方更新时间 |
| matched_building_id | string | 匹配到的建筑记录 |
| match_method | string | exact_address / fuzzy_address / spatial_20m / spatial_50m / unmatched |
| match_distance_m | float | 空间匹配距离 |
| match_confidence | string | High / Medium / Low / Unmatched |

## 41. Pipeline 命令入口

后续开发必须提供命令化入口，避免所有操作依赖 dashboard 手动点击。

建议命令：

```bash
python -m src.data_sources.fetch_bdbiar
python -m src.data_sources.fetch_boundaries
python -m src.data_sources.fetch_mbis
python -m src.pipeline.normalize_buildings
python -m src.pipeline.build_risk_scores
python -m src.pipeline.build_district_metrics
python -m src.pipeline.match_regulatory_notices
python -m src.pipeline.build_all
streamlit run app.py
```

`build_all` 需要按顺序执行：

```text
fetch_bdbiar
  -> normalize_buildings
  -> fetch_boundaries
  -> fetch_mbis
  -> match_regulatory_notices
  -> build_risk_scores
  -> build_district_metrics
  -> write_manifest
```

每个命令都需要支持：

- `--force`：重新下载或重新生成。
- `--use-cache`：优先使用本地缓存。
- `--output-dir`：指定输出目录。
- `--log-level`：控制日志详细程度。

## 42. PR 拆分与依赖顺序

后续批量开发时，不建议一个 PR 同时完成所有东西。建议按依赖顺序推进。

### 42.1 第一批 PR：数据底座

| PR | 内容 | 依赖 |
| --- | --- | --- |
| PR-01 | 项目骨架、requirements、README、基础配置 | 无 |
| PR-02 | 官方 building age 下载、解压、缓存 | PR-01 |
| PR-03 | 字段标准化和楼龄计算 | PR-02 |
| PR-04 | 风险评分 v0.2 | PR-03 |
| PR-05 | district metrics 聚合 | PR-03, PR-04 |

第一批完成后，必须已经能从官方数据生成 `buildings.parquet` 和 `district_metrics.parquet`。

### 42.2 第二批 PR：dashboard 产品基线

| PR | 内容 | 依赖 |
| --- | --- | --- |
| PR-06 | Streamlit 基础页面和导航 | PR-01 |
| PR-07 | Data Intake 页面 | PR-02, PR-03 |
| PR-08 | Portfolio Overview 页面 | PR-05, PR-06 |
| PR-09 | Spatial Dashboard 点位地图 | PR-03, PR-06 |
| PR-10 | District Benchmark 页面 | PR-05, PR-06 |

第二批完成后，必须已经能本地打开 dashboard 并完成核心分析。

### 42.3 第三批 PR：监管信号与 AI 报告

| PR | 内容 | 依赖 |
| --- | --- | --- |
| PR-11 | MBIS issued 数据接入 | PR-01 |
| PR-12 | MBIS 与主表匹配 | PR-03, PR-11 |
| PR-13 | Regulatory Signals 页面 | PR-12 |
| PR-14 | AI prompt builder 与模板摘要 | PR-08, PR-10 |
| PR-15 | LLM 调用和输出审查 | PR-14 |
| PR-16 | Markdown / HTML 报告导出 | PR-14, PR-15 |

第三批完成后，dashboard 从“楼龄分析工具”升级为“维护优先级与报告生成工具”。

### 42.4 第四批 PR：研究验证和交付打磨

| PR | 内容 | 依赖 |
| --- | --- | --- |
| PR-17 | 数据质量与方法页 | PR-03, PR-12 |
| PR-18 | 图表导出和结果导出 | PR-08, PR-10 |
| PR-19 | 验证脚本和测试数据 | PR-05, PR-12 |
| PR-20 | 示例报告、截图、演示说明 | 所有 v1.0 核心 PR |

## 43. 质量门禁

每个 PR 合并前必须满足对应质量门禁。

### 43.1 数据 pipeline 门禁

- 能在无 dashboard 的情况下通过命令生成处理后数据。
- 生成文件路径稳定。
- 处理过程写入 manifest。
- 关键字段缺失时给出明确错误。
- 网络不可用时可使用缓存或明确失败。
- 官方 zip、裸 CSV、用户上传 CSV 的读取逻辑分开处理。

### 43.2 指标计算门禁

- `building_age` 计算规则有单元测试。
- `age_band` 边界值有测试。
- `risk_score` 边界值有测试。
- `district_priority_index` 每个分项可追溯。
- 空数据输入不会导致程序崩溃。

### 43.3 Dashboard 门禁

- 页面首次加载不应依赖重新下载官方数据。
- 所有筛选项默认可用。
- 筛选结果为空时显示空状态。
- 地图无坐标数据时显示说明。
- 图表标题和单位明确。
- 页面不能直接显示 Python traceback。

### 43.4 AI 报告门禁

- 没有 API Key 时必须使用模板摘要。
- LLM 输入只包含聚合指标，不直接发送全量原始表。
- AI 输出必须包含方法限制说明。
- AI 输出数字必须来自输入 JSON。
- 报告导出必须包含数据源版本和模型版本。

## 44. Definition of Done

### 44.1 Phase 0 Done

Phase 0 完成必须满足：

- 一条命令能生成 `buildings.parquet`。
- 输出 manifest 包含数据源、下载时间、记录数、原始文件名。
- `buildings.parquet` 字段满足数据契约。
- 至少 90% 官方记录能计算楼龄。
- 区名、用途、建筑类型分类统计可输出。
- README 说明如何重新生成数据。

### 44.2 Phase 1 Done

Phase 1 完成必须满足：

- `streamlit run app.py` 能启动 dashboard。
- Data Intake、Portfolio Overview、Spatial Dashboard、District Benchmark 可用。
- 18 区筛选、用途筛选、楼龄筛选、风险筛选可用。
- 地图能显示有效点位。
- 区域排名表能按指标排序。
- Markdown 报告能导出。
- 无 API Key 时仍可生成模板摘要。

### 44.3 Phase 2 Done

Phase 2 完成必须满足：

- 能下载并缓存 MBIS issued 数据。
- 能生成 `regulatory_notices.parquet`。
- 能输出 MBIS 匹配率。
- Regulatory Signals 页面可展示通知点位和区域通知密度。
- 风险模型能区分 age-only score 和 combined score。
- AI Report Studio 能生成带监管信号说明的报告。

### 44.4 Phase 3 Done

Phase 3 完成必须满足：

- 能导出研究用图表和表格。
- 能复现 18 区 priority ranking。
- 有验证脚本输出数据质量和模型 sanity check。
- 有示例研究报告。
- 有方法说明，可转写进论文方法章节。

## 45. 自动化测试与验收脚本

### 45.1 最低测试集合

建议至少包含：

```bash
pytest tests/test_normalize_buildings.py
pytest tests/test_risk_rules.py
pytest tests/test_district_metrics.py
pytest tests/test_prompt_builder.py
pytest tests/test_report_generator.py
```

### 45.2 数据验证脚本

建议提供：

```bash
python -m src.pipeline.validate_outputs
```

输出检查：

- `buildings.parquet` 是否存在。
- 必需字段是否存在。
- 记录数是否大于 0。
- 楼龄解析率。
- 坐标有效率。
- 风险等级分布。
- 18 区是否完整。
- manifest 是否存在。

### 45.3 Dashboard 验收脚本

手工验收脚本：

1. 启动 `streamlit run app.py`。
2. 进入 Data Intake，确认数据源和记录数。
3. 进入 Portfolio Overview，确认指标卡和图表出现。
4. 选择 `Yau Tsim Mong`，确认指标刷新。
5. 进入 Spatial Dashboard，确认点位地图出现。
6. 切换风险等级筛选，确认地图和图表同步更新。
7. 进入 District Benchmark，确认 18 区排名表出现。
8. 进入 AI Report Studio，生成模板摘要。
9. 导出 Markdown 报告。
10. 打开导出的报告，确认包含数据源、筛选条件、指标、摘要和限制说明。

## 46. 性能与可用性要求

### 46.1 性能目标

| 场景 | 目标 |
| --- | --- |
| 从 Parquet 加载官方建筑数据 | 5 秒以内 |
| 页面筛选响应 | 2 秒以内 |
| Portfolio Overview 图表刷新 | 3 秒以内 |
| 地图加载 5 万点 | 可采用聚合、抽样或 clustering |
| 报告生成 | 10 秒以内，不含 LLM 网络时间 |

如果点位地图性能不足，优先策略：

1. 默认显示 district choropleth。
2. 点位图按筛选结果显示。
3. 超过阈值时启用 marker clustering。
4. 仍不足时抽样或切换 pydeck。

### 46.2 可用性要求

- 用户第一次打开页面时，不应看到空白或错误堆栈。
- 没有官方数据缓存时，页面应提示下载或使用 sample 数据。
- 每个页面都应显示当前数据版本或分析范围。
- 每个风险图表附近都要说明评分是 rule-based screening。
- 报告生成页要明确区分模板摘要和 LLM 摘要。

## 47. 版本命名与模型治理

### 47.1 数据版本

数据版本建议格式：

```text
bdbiar_YYYYMMDD
mbis_YYYYMMDD
district_boundary_YYYYMMDD
```

### 47.2 风险模型版本

风险模型建议版本：

```text
risk_rules_v0.1_age_only
risk_rules_v0.2_age_usage_type
risk_rules_v0.3_age_regulatory_combined
```

每次修改风险权重，必须记录：

- 修改日期。
- 修改原因。
- 修改前后权重。
- 对 18 区 ranking 的影响。

### 47.3 Prompt 版本

AI prompt 也需要版本号：

```text
summary_prompt_v0.1
summary_prompt_v0.2_with_regulatory_signals
research_report_prompt_v0.1
```

导出报告中必须记录 prompt 版本，避免后续研究不可复现。

## 48. 当前文档深度结论

补充到本章后，需求文档已经达到可以进入批量开发的深度。

可以直接启动的工作：

- Phase 0 数据底座。
- Phase 1 Dashboard 产品基线。
- Phase 2 MBIS 监管信号增强。
- AI 摘要和报告生成。

后续开发时，建议不要再继续无限扩需求。下一步应进入实现，并在实现过程中只针对真实数据问题、小范围交互问题和验证结果做迭代。

## 49. 配套技术文档索引

为了支持后续批量 PR 和自动化开发，除本需求文档外，项目还需要以下执行类文档。后续开发时，应按以下顺序阅读：

1. `需求文档.md`：理解项目目标、产品范围、研究背景和总体架构。
2. `docs/04_v1成品交付规格.md`：理解 v1.0 成品到底交付什么、哪些状态不能算完成。
3. `docs/00_技术执行规则.md`：理解后续开发必须遵守的执行规则、模块边界和 PR 约束。
4. `docs/01_数据指标口径.md`：理解官方数据字段映射、楼龄计算、风险评分、区域指标和数据质量口径。
5. `docs/02_开发实施路线图.md`：理解 Phase 0-4 的 PR 拆分、依赖顺序和每个 PR 的交付物。
6. `docs/03_验收测试清单.md`：理解每个阶段如何测试、验收和判断是否可以进入下一阶段。
7. `docs/05_系统架构与部署运维.md`：理解 local-first 架构、运行模式、部署和数据更新策略。
8. `docs/06_UI交互规格.md`：理解 v1.0 dashboard 的页面、组件、状态和交互要求。
9. `docs/08_产品体验优化规划.md`：理解下一版体验增强的用户问题、设计方向、PR 顺序和验收标准。

### 49.1 文档优先级

如果早期需求描述与执行类文档存在细节冲突，优先级如下：

```text
docs/04_v1成品交付规格.md
  > docs/01_数据指标口径.md
  > docs/00_技术执行规则.md
  > docs/05_系统架构与部署运维.md
  > docs/06_UI交互规格.md
  > docs/08_产品体验优化规划.md
  > docs/02_开发实施路线图.md
  > docs/03_验收测试清单.md
  > 需求文档.md 中的早期概念性描述
```

原因是早期章节用于定义方向，后续执行文档用于冻结实现规则。

### 49.2 当前是否可以进入开发

可以。

当前文档体系已经覆盖：

- 产品目标。
- 真实官方数据源。
- 数据字段映射。
- 数据契约。
- 风险评分口径。
- 区域指标口径。
- dashboard 页面结构。
- AI 报告边界。
- PR 执行顺序。
- 质量门禁。
- 自动化测试和人工验收。

该判断用于初始开发启动阶段：不应长期停留在需求扩写，而应先进入 Phase 0 数据底座开发。按照当前 2026-06-06 的实现状态，v1.0 功能闭环已经跑通，下一步应进入 Phase 4 产品体验增强。

## 50. v1.0 成品口径确认

本项目后续开发目标不是“只做一个 MVP”，而是直接交付 v1.0 成品。

这里的 v1.0 成品定义为：

- 本地可运行的研究级 dashboard 产品。
- 能接入、缓存和处理香港官方公开数据。
- 能完成从数据读取、清洗、指标计算、风险评分、地图图表、监管信号、AI/模板报告到导出的完整流程。
- 能通过测试和验收清单。
- 能被用户按 README 独立运行。
- 能生成可用于研究汇报的 Markdown / HTML 报告。

Phase 0-3 是为了降低工程风险的实施顺序，不是降低交付目标。最终交付必须满足 `docs/04_v1成品交付规格.md`。

## 51. 当前版本体验复盘与下一版优化方向

截至 2026-06-06，当前产品已经形成数据、分析、地图、监管信号、报告和导出包的完整闭环。下一版开发重点不应继续扩散底层功能，而应集中解决真实试用中暴露出的体验问题。

### 51.1 当前体验问题

当前版本的主要问题：

- 用户首次打开后缺少清晰入口，不知道应该从哪个页面开始。
- 页面结构偏模块化，适合开发者理解，但不够贴近真实任务流。
- 全局中英文体验不统一，目前主要体现在报告页，未覆盖 dashboard 主体。
- Data Intake 中技术信息偏多，manifest、JSON 和路径容易压过业务判断。
- 页面视觉偏干，指标、状态、说明和行动按钮的层次还不够清晰。
- AI Report Studio 具备功能，但还不像正式报告工作台。
- DeepSeek / OpenAI 等 LLM 配置还没有管理员面板，真实 API 测试流程不够产品化。
- 地图能展示数据，但还需要更明确的图层、抽样、clustering 和 tooltip 设计。
- 缺少 dashboard 截图、演示脚本和面向研究汇报的体验材料。

这些问题不代表核心产品不可用，而是说明产品需要从“工程可运行”继续走向“用户愿意用、研究团队容易演示”。

### 51.2 下一版产品目标

下一版体验增强目标：

- 新用户打开 30 秒内能判断数据状态和下一步操作。
- 用户能按任务进入分析，而不是先理解所有技术模块。
- 中英文界面体验一致，报告语言可以独立控制。
- 数据源和方法限制可见，但技术细节不干扰主流程。
- 报告生成从摘要按钮升级为结构化报告工作台。
- 管理员可以在界面中配置 DeepSeek / OpenAI，并测试连接。
- 地图在 5 万级官方数据上保持可理解、可操作。
- 导出报告和导出包可直接用于研究汇报。

### 51.3 下一版新增或强化模块

| 模块 | 目标 | 说明 |
| --- | --- | --- |
| Home / Start | 新手入口 | 展示数据状态、推荐任务和可用导出物 |
| Global Language Switch | 全局语言体验 | 顶部或侧边栏提供 `中文 / EN` 或语言 icon |
| Data Intake 体验重构 | 降低技术噪音 | 默认显示数据健康卡，manifest 和字段细节折叠 |
| Admin / Settings | LLM 管理 | DeepSeek / OpenAI / Template 配置、连接测试、fallback 状态 |
| AI Report Studio 工作台化 | 报告生成体验 | 报告范围确认、结构化预览、导出检查 |
| Spatial Dashboard 优化 | 地图可理解性 | 图层开关、抽样提示、tooltip 资产卡片 |
| Visual System | 专业视觉风格 | 状态标签、指标解释、按钮层级、页面说明统一 |
| Demo Materials | 研究汇报交付 | 截图、5 分钟演示脚本、示例中英文报告 |

### 51.4 下一版优先级

P0：

- Home / Start 页面。
- 全局中英文切换。
- Data Intake 数据健康视图。
- Admin / Settings 的 DeepSeek / OpenAI 配置。
- AI Report Studio 报告工作台化。

P1：

- Spatial Dashboard 图层和 tooltip 优化。
- 全站视觉系统和文案统一。
- Method & QA 研究可信度表达增强。
- Dashboard 截图和 5 分钟演示流程。

P2：

- PDF 报告。
- 更强地图引擎。
- 多用户权限。
- 长期在线部署。

### 51.5 下一版验收标准

下一版体验增强完成后，应满足：

- 用户从首页可以进入全港、单区、用途专题、数据质量审查和报告生成。
- 全局中英文切换覆盖主要页面标题、按钮、状态和说明。
- DeepSeek API Key 可在 Admin / Settings 配置并测试，失败时自动 fallback Template。
- AI Report Studio 在生成前显示筛选条件、记录数、数据版本、模型版本和 prompt 版本。
- 地图明确展示当前是区域层、点位层、抽样还是 clustering。
- Data Intake 默认能让非技术用户判断当前数据是否可用。
- Method & QA 继续保留技术复现信息，但不干扰新手主流程。
- 导出包、Markdown 报告、HTML 报告和演示材料可以支撑一次研究汇报。

详细体验规划以 `docs/08_产品体验优化规划.md` 为准；具体 UI 组件和页面状态以 `docs/06_UI交互规格.md` 为准。

### 51.6 下一步执行口径

PR-21 Home / Start 已作为 Phase 4 的入口体验先行落地。下一步不再按很小的单点改动推进，而应按 Sprint A 连续完成以下产品切片：

- PR-22 全局中英文切换。
- PR-23 Data Intake 体验重构。
- PR-24 Admin / Settings 面板。
- PR-25 AI Report Studio 工作台化。

这四项应作为同一轮成熟产品体验冲刺处理，目标是让用户能用统一语言理解数据状态、配置 DeepSeek / OpenAI、确认报告范围，并完成模板或 LLM 报告导出。

Sprint A 不改变已验证正常的官方数据 pipeline、风险模型和核心报告导出契约。完成后必须通过 `python -m pytest` 和 `python -m src.pipeline.product_readiness`，并可做一次阶段性 git checkpoint。
